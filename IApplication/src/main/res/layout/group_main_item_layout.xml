<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/main_view_round_padd">

    <TextView
        android:id="@+id/user"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:ellipsize="end"
        android:maxWidth="200dp"
        android:singleLine="true"
        android:textColor="@color/main_view_user_color"
        android:textSize="@dimen/main_view_user_size"/>

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/user"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="7dp"
        android:layout_toLeftOf="@id/user"
        android:ellipsize="end"
        android:singleLine="true"
        android:textColor="@color/main_view_title_color"
        android:textSize="@dimen/main_view_title_size"/>

    <TextView
        android:id="@+id/description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/title"
        android:layout_marginTop="7dp"
        android:ellipsize="end"
        android:maxLines="3"
        android:singleLine="false"
        android:textColor="@color/main_view_mark_color"
        android:textSize="@dimen/main_view_description_size"/>

    <TextView
        android:id="@+id/mark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/description"
        android:layout_marginTop="7dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:singleLine="false"
        android:textColor="@color/main_view_mark_color"
        android:textSize="@dimen/main_view_mark_size"/>
</RelativeLayout>